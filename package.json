{
  "name": "understanding-react-esmodules-2",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "strip-flow-types": "npx flow-remove-types --out-dir dist/ packages/",
    "build-bundle": "NODE_PATH=dist npx esbuild src/main.js --bundle --outfile=bundle.js --sourcemap --format=esm --target=esnext --define:__DEV__=false",
    "compile-react": "npm run strip-flow-types && npm run build-bundle"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "0.25.4",
    "esbuild-plugin-flow": "^0.3.2",
    "flow-bin": "^0.271.0",
    "flow-remove-types": "^2.271.0",
    "vite": "^6.3.5"
  }
}
