{
  "name": "understanding-react-esmodules-2",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "strip-flow-types": "npx flow-remove-types --out-dir dist/ packages/",
    "build-bundle": "node esbuild.mjs",
    "compile-react": "npm run strip-flow-types && npm run build-bundle",
    "compile-watch": "nodemon --watch src --watch packages --ext js,jsx --exec \"npm run compile-react\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "0.25.4",
    "flow-remove-types": "^2.271.0",
    "nodemon": "^3.1.10"
  }
}
