export default function FrenchCounter({ name }) {
    return (
      <article>
        <h2>Compteur {name}</h2>
        <p>Bouton appuyé 1 fois</p>
        <button>Incrementer</button>
      </article>
    );
}